{
  "name": "@leopard/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "dev": "pnpm --filter @leopard/shared build && expo start",
    "build": "pnpm --filter @leopard/shared build && expo export --platform android --output-dir dist",
    "lint": "pnpm --filter @leopard/shared build && pnpm --dir ../.. exec eslint apps/mobile/src apps/mobile/App.tsx apps/mobile/index.ts",
    "test": "pnpm --filter @leopard/shared build && pnpm --dir ../.. exec vitest run apps/mobile/src",
    "typecheck": "pnpm --filter @leopard/shared build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@leopard/shared": "workspace:*",
    "expo": "^57.0.2",
    "react": "^19.2.7",
    "react-native": "^0.86.0",
    "react-native-safe-area-context": "^5.8.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "react-dom": "^19.2.7",
    "typescript": "^5.9.3"
  }
}
