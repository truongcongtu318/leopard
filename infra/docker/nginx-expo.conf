# Shared Nginx config for Expo Web SPA apps (customer / driver)
# Serves static files from /usr/share/nginx/html with SPA fallback
# Proxies /api/v1/ and /socket.io/ to backend API

server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback.
    #
    # no-store on the HTML is deliberate: index.html is what points at the
    # content-hashed bundle, so a browser that keeps serving a cached copy keeps
    # running whatever JavaScript it referenced at the time — including a build
    # from before a fix. The hashed assets under /_expo/ keep their long cache
    # below, which is where caching actually pays off.
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store";
    }

    # Proxy API calls to backend
    location /api/v1/ {
        proxy_pass http://api:3000/api/v1/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 20M;
    }

    # Proxy static file serving (local storage uploads)
    location /files/ {
        proxy_pass http://api:3000/files/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Proxy Socket.IO (WebSocket upgrade)
    location /socket.io/ {
        proxy_pass http://api:3000/socket.io/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 86400;
    }

    # Cache static assets
    location /_expo/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
